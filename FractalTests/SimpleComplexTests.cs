using Fractal;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace FractalTests
{
	[TestClass]
	public class SimpleComplexTests
	{
		[TestMethod]
		public void multiplies()
		{/*
			var expected = new SimpleComplex(20, -8);
			var actual = new SimpleComplex(2, 2).Mult(new SimpleComplex(3, 7));
			Assert.AreEqual(expected.Real, actual.Real);
			Assert.AreEqual(expected.Imaginary, actual.Imaginary);*/
		}
	}
}
